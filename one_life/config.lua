config = {}


config.settings = {
    framework = 'standalone', -- easy to convert to a framework [Renember it uses oxmysql!]
    lives = 1, -- gets banned from sever after 1 death
}


-- SCRIPT DOSNT KEEP PLAYER OUT BUT I HAVE ADD A COMMENT WHERE YOU CAN LOG THE BAN AND KEEP THEM OUT!